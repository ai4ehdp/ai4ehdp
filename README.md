## Document classification

This branch contains source code for working with models (training and inference) capable of classifying handwritten documents into categories: plaintext, ciphertext, mixed content and cipher key. Additionally, a fully trained model in `.pt` format used for classification within the project is stored here.
